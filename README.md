# 🖥️ Browser Virtual File System (BVFS)

> **Note:** This project and its underlying architecture were 100% generated by an AI engineering assistant. It was built as an advanced, zero-dependency portfolio piece to demonstrate deep browser APIs, persistent storage, and client-side systems engineering.

BVFS is a fully functional, POSIX-like virtual operating system and shell that runs entirely in your web browser. 

It requires **no backend, no Node.js, and no servers**. It persists data directly to your browser's hard drive using the Origin Private File System (OPFS), supports executing WebAssembly binaries, and can even mount real folders from your host machine into the browser terminal.

---

## ✨ Key Features

* **💾 Persistent Storage (OPFS):** Your files, directories, and command history survive page refreshes. It acts like a real hard drive sandboxed inside your browser.
* **🔌 The Mount System:** Use the File System Access API to mount a real folder from your Windows/Mac/Linux machine directly into the virtual `/mnt/local` directory.
* **⚙️ WebAssembly (WASM) Engine:** Drop a compiled `.wasm` binary into the filesystem and execute it natively from the terminal. The shell intercepts its standard output automatically.
* **🌐 Network Bridge:** A built-in `wget` command allows you to download files and binaries from the internet directly into your virtual filesystem.
* **📜 Shell Scripting:** Write `.sh` files and execute them line-by-line.
* **🔄 Background Jobs:** Run long-running commands (like `sleep 10`) in the background using `&` without freezing the terminal.
* **📦 Micro-Kernel Architecture:** The entire OS runtime, VFS router, parser, and command registry are contained within a single, highly optimized `bvfs.js` file.

---

## 🚀 Getting Started

Because the Origin Private File System (OPFS) requires a secure context, you cannot run this by simply double-clicking the `index.html` file. 

**Option 1: GitHub Pages (Recommended)**
1. Fork or clone this repository.
2. Enable GitHub Pages in your repository settings.
3. Visit your live URL. OPFS is enabled automatically over HTTPS.

**Option 2: Localhost**
Run any local web server in the root directory:
```bash
# Using Python
python3 -m http.server 8000

# Using Node.js
npx http-server
